on:
  workflow_call:
    inputs:
      dotnet-version:
        type: string
        required: true

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Configure GitHub Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - name: Install, build, and upload documentation site
        uses: withastro/action@44706356b4eb735f8b9035699eb4796241a040c4 # v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path: docs/astro
#       - name: Validate links
#         uses: lycheeverse/lychee-action@v2
#         with:
#           args: --github-token ${{ secrets.GITHUB_TOKEN }} --base-url dist --exclude-all-private --exclude 404 --include-fragments dist
#           workingDirectory: docs/astro
#           lycheeVersion: nightly
      - name: Deploy GitHub Pages site
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5

  docfx-bug-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ inputs.dotnet-version }}
      # Alternative: Install DocFX nightly from GitHub Packages (commented out)
      # - name: Install DocFX nightly (GitHub Packages)
      #   shell: pwsh
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.PAT_READ_PACKAGES }}
      #   run: |
      #     echo $env:GITHUB_TOKEN | gh auth login --with-token
      #     gh auth refresh -h github.com -s read:packages
      #
      #     $latestDocfxVersion = gh api /orgs/dotnet/packages/nuget/docfx/versions --jq '.[0].name'
      #     $docfxDownloadUrl = "https://nuget.pkg.github.com/dotnet/download/docfx/$latestDocfxVersion/$latestDocfxVersion.nupkg"
      #     $docfxLocalPackageFile = "docfx.$latestDocfxVersion.nupkg"
      #
      #     Invoke-RestMethod -Uri $docfxDownloadUrl -OutFile $docfxLocalPackageFile -Headers @{ Authorization = "Bearer $env:GITHUB_TOKEN" }
      #     dotnet tool update -g docfx --prerelease --source ./
      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: '24'
      - name: Clone DocFX repository
        run: |
          git clone https://github.com/dotnet/docfx.git
          cd docfx
          git fetch origin refs/pull/10561/head:pr-10561
          git checkout pr-10561
      - name: Build DocFX templates
        run: |
          cd docfx/templates
          npm install
          npm run build
      - name: Build and pack DocFX
        run: |
          cd docfx
          dotnet build
          dotnet pack src/docfx/docfx.csproj -c Release -o ./artifacts/nupkg
      - name: Install DocFX from local package
        run: dotnet tool install -g docfx --add-source ./docfx/artifacts/nupkg --prerelease
      - name: Build with DocFX
        run: docfx docs/docfx/docfx.json --verbose
        continue-on-error: true
        
